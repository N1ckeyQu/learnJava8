package com.learn;

/**
 * 继承，在Java中通过extends关键字实现。如果class A extends B
 * 那么A就继承了B中所有的属性和方法（不包括构造器）
 * 当然，由于封装性的存在，A不一定能访问B全部的属性和方法
 *
 * 在Java中，我们将A称为子类、派生类、subclass
 * B称为父类、超类、基类、superclass
 *
 * 继承的优点在于提高了代码的复用性，便于功能的扩展，并且为之后的多态性提供了前提
 *
 * Java中关于继承有一些规定：
 * 1、一个类可以被多个子类继承
 * 2、类具有单继承性：一个类只能有一个父类
 * 3、父子类是相对的概念，因为Java允许多层继承。对于A继承B、B继承C这种情况，B是A的直接父类，C是A的间接父类
 * A拥有了直接父类B和间接父类C的属性和方法
 */
public class ExtendsTest {

}
